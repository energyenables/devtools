// This file is imported by test files to set up Jest DOM matchers
import '@testing-library/jest-dom';

// This empty test ensures the file doesn't fail for having no tests
test('setup file loads correctly', () => {
  expect(true).toBe(true);
});
